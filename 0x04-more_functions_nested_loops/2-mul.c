#include "main.h"

/**
 * mul - multiply two numbers
 * @a: first integer to multiply
 * @b: second integer to multiply
 * Return: value
 */

int mul(int a, int b)
{
	int times;

	times = (a * b);

	return (times);
}
